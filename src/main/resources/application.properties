spring.application.name=inventario-repuestos-backend
# Use MONGODB_URI env var if present, otherwise default to local DB 'facturacion-inventario'
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/facturacion-inventario}

# JWT secret: prefer APP_JWT_SECRET env var; default is unsafe placeholder for dev only
app.jwt.secret=${APP_JWT_SECRET:replace_this_secret_change_in_prod}
# Allowed CORS origins (comma-separated). If empty, defaults to http://localhost:3000 for development
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:}
APP_ADMIN_REGISTRATION_KEY=miClaveSecretaMuyFuerte123!
# --- secrets import ---
# Load sensitive values from an optional local file `application-secrets.properties`
# Do NOT commit `application-secrets.properties` into source control. Instead create
# `src/main/resources/application-secrets.properties` locally or set equivalent env vars.
spring.config.import=optional:classpath:application-secrets.properties

# Local secrets (DO NOT commit)
APP_OAUTH_GOOGLE_CLIENT_ID=926718099758-osi38sedrsjvil21a10f1eq25ub0q351.apps.googleusercontent.com

# Facebook
APP_OAUTH_FACEBOOK_APP_ID=725126533892938
APP_OAUTH_FACEBOOK_APP_SECRET=8d0c9f04d6bd2620a5ae5103b6d22c02
FACEBOOK_CLIENT_TOKEN=639e3d91cfc30af0f1d2e8d66b1bf2a9